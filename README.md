▶️ Setup instructions (standard Python)
Create virtual environment
python3 -m venv venv

Activate it
source venv/bin/activate

Install dependencies
pip install -r requirements.txt

🧪 Verify
python -c "import requests; print('OK')"